require 'spec_helper'

describe Tradenity do
  it 'has a version number' do
    expect(Tradenity::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
